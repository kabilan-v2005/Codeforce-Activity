import java.io.*;import java.util.*; public class Main {     public static long flip_flop(int n, long c, long k, long[] arr) {        Arrays.sort(arr);         for (int i = 0; i < n; i++) {            if (arr[i] > c) break;             long increase = Math.min(k, c - arr[i]);            arr[i] += increase;            k -= increase;             c += arr[i];        }        return c;    }     public static void main(String[] args) throws Exception {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));         int t = Integer.parseInt(br.readLine());         while (t-- > 0) {            String[] first = br.readLine().split(" ");            int n = Integer.parseInt(first[0]);            long c = Long.parseLong(first[1]);            long k = Long.parseLong(first[2]);             long[] arr = new long[n];             String[] second = br.readLine().split(" ");            for (int i = 0; i < n; i++) {                arr[i] = Long.parseLong(second[i]);            }             System.out.println(flip_flop(n, c, k, arr));        }    }}